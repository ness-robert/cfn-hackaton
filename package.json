{
    "name": "robert-webhookconfig-repository",
    "version": "0.1.0",
    "description": "AWS custom resource provider named Robert::WebhookConfig::Repository.",
    "private": true,
    "main": "dist/handlers.js",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "npx tsc",
        "prepack": "npm run build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@amazon-web-services-cloudformation/cloudformation-cli-typescript-lib": "^1.0.1",
        "class-transformer": "0.3.1",
        "node-fetch": "^2.6.1"
    },
    "devDependencies": {
        "@types/node": "^12.0.0",
        "@types/node-fetch": "^2.5.10",
        "typescript": "^4.1.2"
    },
    "optionalDependencies": {
        "aws-sdk": "^2.656.0"
    }
}
